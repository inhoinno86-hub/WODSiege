import type { CapacitorConfig } from "@capacitor/cli";
const config: CapacitorConfig = {
  appId: "app.wodsiege.pilot",
  appName: "WODSiege",
  webDir: "dist",
  android: { backgroundColor: "#0b1119" },
};
export default config;
